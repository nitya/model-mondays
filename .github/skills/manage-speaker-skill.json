{
  "name": "manage-speaker",
  "displayName": "Manage speaker metadata",
  "description": "Adds or updates source-backed speaker records without external profile scraping.",
  "version": "2.0.0",
  "parameters": {
    "action": {
      "type": "enum",
      "required": true,
      "options": ["add", "update"]
    },
    "id": {
      "type": "string",
      "required": true,
      "pattern": "^[a-z0-9-]+$"
    },
    "name": {
      "type": "string",
      "required": true
    },
    "role": {"type": "string", "required": false},
    "affiliation": {"type": "string", "required": false},
    "bio": {"type": "string", "required": false},
    "profileImage": {"type": "string", "required": false},
    "socialLinks": {"type": "object", "required": false},
    "expertise": {"type": "array", "required": false, "items": {"type": "string"}}
  },
  "rules": [
    "use only information explicitly supplied by an approved source",
    "do not scrape LinkedIn or infer missing profile fields",
    "profile images must be approved standalone assets under docs/assets/people",
    "empty optional fields are allowed",
    "preserve unrelated speaker fields during updates",
    "run repository validation"
  ]
}
