{
  "name": "update-content",
  "displayName": "Update Model Mondays or AMA content",
  "description": "Updates existing content while keeping JSON, pages, indexes, and assets consistent.",
  "version": "2.0.0",
  "parameters": {
    "contentType": {
      "type": "enum",
      "required": true,
      "options": ["episode", "ama"]
    },
    "id": {
      "type": "string",
      "required": true,
      "description": "Episode ID such as s4-e07 or AMA ID such as ama-048"
    },
    "updateType": {
      "type": "enum",
      "required": true,
      "options": [
        "update-title",
        "update-description",
        "update-speakers",
        "add-resources",
        "add-links",
        "update-banner",
        "add-recap"
      ]
    },
    "content": {
      "type": "object",
      "required": true
    }
  },
  "rules": [
    "locate content by stable ID, not by a season relationship",
    "preserve unrelated content",
    "update the JSON record and Markdown page together",
    "update public indexes when date, title, speakers, status, or page path changes",
    "verify new assets and links before referencing them",
    "do not add placeholders",
    "run repository validation"
  ]
}
